1、填空题
    1、在正则 表达式中\d代表什么()
        --数字字符
    2、在java程序中异常处理的关键字是()，抛出异常的关键字是()
        --try catch finally
        --throw  throws
    3、java中编程实现多线程应用两种途径：一种是通过()创建线程，另一种是通过创建()接口创建线程
        --创建Thread类的子类
        --Runnable
    4、表达式1/2*9的计算结果是()。
        --0
    5、JAVA提供了三个日期类，他们分别是()、()和Date。
        --Calendar
        --DateFormate
  
02、问答
    1、抽象的定义？抽象和封装的不同点？
        --
    2、Overload 和 Override 的区别。 Overloaded 的方法是否可以改变返回值的类型 ?
    
03、编程
    1、所谓“水仙花”数是指一个3位数，其各位数字立方和等于该数本身。例如，153就是一个水仙花数，因为153=1*1*1+5*5*5+3*3*3。
          题目要求 ：
              输入一个3位数整数，检查是否为“水仙花”数。是水仙花数显示为Yes，否则显示No 
          输入描述：
              输入一个3位数整数
          输出描述：
              是水仙花数显示为Yes，否则显示No
              
          示例：
              输入：153   输出：Yes
              
    2、给定数组和int值，返回数组中两数相加之和等于int值的下标。要求时间复杂度O(n)
          示例：
              nums = [2, 7, 11, 15], target = 9
              nums[0] + nums[1] = 2 + 7 = 9
              return [0, 1] 
              
04、单选题
    1、
        有以下类定义：
            abstract class Animal{
            abstract void say();
            }
            public class Cat extends Animal{
             public Cat(){
              System.out.printf("I am a cat");
             }
             public static void main(String[] args) {
              Cat cat=new Cat();
             }
            }
         运行后
            A、I am a cat
            B、Animal能编译，Cat不能编译
            C、Animal不能编译，Cat能编译
            D、编译能通过，但是没有输出结果
            
         正确答案：D
    2、如果一个方法或变量是"private"访问级别，那么它的访问范围是:
            A、在当前类，或者子类中
            B、在当前类或者它的父类中
            C、在当前类，或者它所有的父类中
            D、在当前类中
         正确答案：D
    3、instanceof运算符能够用来判断一个对象是否为:
            A、一个类的实例
            B、一个实现指定接口的类的实例
            C、全部正确
            D、一个子类的实例
         正确答案：C
    4、
            A、
            B、
            C、
            D、
         正确答案：
    5、
            A、
            B、
            C、
            D、
         正确答案：
    6、
            A、
            B、
            C、
            D、
         正确答案：
    7、
            A、
            B、
            C、
            D、
         正确答案：
    8、
            A、
            B、
            C、
            D、
         正确答案：
    9、
            A、
            B、
            C、
            D、
         正确答案：
    10、
            A、
            B、
            C、
            D、
         正确答案：
    11、
            A、
            B、
            C、
            D、
         正确答案：
    12、
            A、
            B、
            C、
            D、
         正确答案：
    13、
            A、
            B、
            C、
            D、
         正确答案：
    14、
            A、
            B、
            C、
            D、
         正确答案：
    15、
            A、
            B、
            C、
            D、
         正确答案：
    16、
            A、
            B、
            C、
            D、
         正确答案：
    17、
            A、
            B、
            C、
            D、
         正确答案：
    18、
            A、
            B、
            C、
            D、
         正确答案：
    19、
            A、
            B、
            C、
            D、
         正确答案：
    20、
            A、
            B、
            C、
            D、
         正确答案：
    21、
            A、
            B、
            C、
            D、
         正确答案：
    22、
            A、
            B、
            C、
            D、
         正确答案：
    23、
            A、
            B、
            C、
            D、
         正确答案：
    24、
            A、
            B、
            C、
            D、
         正确答案：
    25、
            A、
            B、
            C、
            D、
         正确答案：

    
